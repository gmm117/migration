// define([  
// ], function () {
    var calc = function() {

    }

    calc.prototype.sum = function(prev, curr) {
        return prev + curr;
    };

    export default calc; // return calc;
// });