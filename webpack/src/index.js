import '../index.css';

//define([
import _calc from 'calc';
import _app from 'app';

//], function (_calc, _app) {
    var calc = new _calc();
    var app = _app;

    console.log(calc.sum(1, 2));
    console.log(app.GetName());
//});
